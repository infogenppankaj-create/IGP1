<!-- Start content -->
<div class="content">
    <div class="container-fluid">
        <label for="inputCity" class="col-form-label"></label>
        <!-- Form row -->
        <div class="card-box">
        <div class="row">
            <div class="col-md-6">
                <div class="row">
                    <div class="col-md-12">
                
                    {{!-- <form> --}}
                        <div class="form-row">
                            <div class="form-group col-md-6" hidden>
                                <label for="Name" class="col-form-label">Add/Updatep Package</label>
                                <select class="form-control select2" id="cmbAU" name="cmbAU_n">
                                    <option>Add Package</option>
                                    <option>Update Package</option>
                                </select>
                            </div>

                            

                        </div>                       

                        <div class="form-row">
                            <div class="form-group col-md-12">
                                <div class="form-row">  
                                    <div class="form-group col-md-12">
                                        <label for="Name" class="col-form-label">Package Name<span class="text-danger">*</span></label>
                                        <select id="cmbPackageName" name="cmbPackageName_n" class="form-control select2"></select>                                        
                                    </div>
                                </div>

                                <div class="form-row"> 

                                    <div class="form-group col-md-12">
                                        <label for="Name" class="col-form-label">Service Name<span class="text-danger">*</span></label>
                                        <select id="cmbServiceName" name="cmbServiceName_n" class="form-control select2"></select>
                                    </div>
                                </div>
                            
                            </div>

                            <div class="form-group col-md-12">
                                <div class="form-group">
                                    <label for="Description" class="col-form-label">Description</label>
                                    <textarea class="form-control" style="height: 100px;" id="txtDescription" name="txtDescription_n" rows="5"
                                        placeholder=""></textarea>
                                </div>

                                
                                <div class="form-group col-md-12">
                                    <label for="Status" class="col-form-label">Status</label>
                                    <select id="cmbStatus" name="cmbStatus_n" class="form-control select2"></select>
                                </div>

                                <div class="form-row">
                                    <label for="Description" class="col-form-label">&nbsp;</label>
                                    <button id="btnsave" type="button" style="width: 100%;font-size: 18px;"
                                        class="btn btn-primary btn-rounded w-md waves-effect waves-light">Save</button>
                                </div>
                            </div>
                        </div>
                    {{!-- </form> --}}
                       
                    </div>
                </div>
            </div>

            <div class="col-md-6">
                <div class="row">
                    <div class="form-group col-md-12">
                        <label for="Name" class="col-form-label">Select Package</label>
                        <select id="cmbSelect" name="cmbSelect_n" class="form-control select2"></select>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="card-box table-responsive">
                            <h4 class="m-t-0 header-title"><b>Department Details</b></h4>
                            <p class="text-muted font-13 m-b-30">

                            </p>
                            <table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
                                <thead>
                                    <tr>
                                        <th style="width:5%">ID</th>
                                        <th>PACKAGE NAME</th>
                                        <th>SERVICE NAME </th>
                                        <th>STATUS</th>
                                    </tr>
                                </thead>
                                <tbody>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        </div>
        </div>

        <!-- end row -->

    </div> <!-- container -->
</div> <!-- content -->
<footer class="footer text-right">
</footer>

<script>
    var defaultdata = {{{ data1 }}};
    $('#lblFormName').html("SubDepartment");
    
    //populate dropdown start
    var option = '';
    for (var i = 0; i < defaultdata.result.length; i++) {
        option += '<option value="' + defaultdata.result[i].ID + '">' + defaultdata.result[i].NAME + '</option>';
    }
    $('#cmbStatus').append(option);

    //
    var option = '';
    option += '<option value="0">SELECT</option>';
    for (var i = 0; i < defaultdata.PackageList.length; i++) {
        option += '<option value="' + defaultdata.PackageList[i].ID + '">' + defaultdata.PackageList[i].NAME + '</option>';
    }
    $('#cmbPackageName').append(option);
    $('#cmbSelect').append(option);

    var option = '';
    option += '<option value="0">SELECT</option>';
    for (var i = 0; i < defaultdata.ServiceForPackage.length; i++) {
        option += '<option value="' + defaultdata.ServiceForPackage[i].ID + '">' + defaultdata.ServiceForPackage[i].NAME + '</option>';
    }
    $('#cmbServiceName').append(option);
    //populate dropdown end   

    //
    $('#cmbSelect').attr('disabled', false);
    $('#cmbStatus').attr('disabled', false);
   
    //
    /*.
    $(function () {
        $("#cmbAU").change(function () {
            if ($("#cmbAU").val() === 'Add Department') { //Update Employee
                $('#cmbSelect').attr('disabled', true);
                $('#cmbStatus').attr('disabled', true);
                $("#cmbSelect").val(0).change();
                $("#cmbStatus").val(1).change();
            } else {
                $('#cmbSelect').attr('disabled', false);
                $('#cmbStatus').attr('disabled', false);
                $("#cmbSelect").val(0).change();
                $("#cmbStatus").val(1).change();
            }
        });
    });
    */
    // Data bound after Combo selection option
    $(function () {        
        $("#cmbSelect").change(function () {
            $('#lblError').html('');
            bindGrid();
            $("#example").dataTable().fnDestroy();
            //bindData()
        });
    });

    //Populate Grid data after form load
    $(document).ready(function () {
        bindGrid();
        $("#example").dataTable().fnDestroy();
    });

    //Save event
    $("#btnsave").on("click", function () {           
            $('#lblError').html('');
            var data = {
                "IN_USER_ID": "0",
                "IN_BRANCH_ID": "0",
                "IN_ID": $("#cmbSelect").val(),
                "IN_PACKAGE_ID": $("#cmbPackageName").val(),
                "IN_SERVICE_ID": $("#cmbServiceName").val(),
                "IN_DESCRIPTION": $("#txtDescription").val(),
                "IN_STATUS": $("#cmbStatus").val(),
            }
            //console.log("data",  data)
            $.ajax({
                url: 'savePackage',
                type: "POST",
                data: JSON.stringify(data),
                dataType: 'json',
                contentType: 'application/json',
                success: function (data) {
                    if (data.message == 0)
                    {
                        alert("Save Error!!");   
                        return false;                     
                    }
                    else
                    {
                        alert("Save Successfully.");
                        window.location.reload(true);
                    } 
                },
                error: function (jqXHR, exception) {
                    var msg = '';
                    if (jqXHR.status === 0) {
                        msg = 'Not connect.\n Verify Network.';
                    } else if (jqXHR.status == 404) {
                        msg = 'Requested page not found. [404]';
                    } else if (jqXHR.status == 500) {
                        msg = 'Internal Server Error [500].';
                    } else if (exception === 'parsererror') {
                        msg = 'Requested JSON parse failed.';
                    } else if (exception === 'timeout') {
                        msg = 'Time out error.';
                    } else if (exception === 'abort') {
                        msg = 'Ajax request aborted.';
                    } else {
                        msg = 'Uncaught Error.\n' + jqXHR.responseText;
                    }
                    alert(msg);
                    return false;
                }
            });       
           
    });
   
    // Populate Grid Data 
    /*
    function bindGrid() {
        var data = {
            IN_USER_ID: "0",
            IN_BRANCH_ID: "0",
            IN_ID: "0",
            IN_PACKAGE_ID: $("#cmbSelect").val(),
            IN_STATUS: "0"
        };
        $.ajax({
            url: 'getPackageGrd',
            type: "POST",
            data: JSON.stringify(data),
            dataType: 'json',
            contentType: 'application/json',
            success: function (data) {
                if (data.message == 0)
                    alert("Invalid User.");
                else {
                    dataSet = data;
                    $('#example').DataTable({
                        "order": [1, "asc"], // Order data                 
                        data: dataSet,
                        columns: [                        
                            { data: "ID" },
                            { data: "PACKAGE"},
                            { data: "SERVICE"},
                            { data: "STATUS"},
                        ],
                        "columnDefs": [
                            {
                                "targets": [0],
                                "visible": false,
                                //"searchable": false
                            }
                        ]                              
                    });
                    var table = $('#example').DataTable();

                    $('#example tbody').on('click', 'tr', function () {
                        if ($(this).hasClass('selected')) {
                            $(this).removeClass('selected');
                        }
                        else {
                            table.$('tr.selected').removeClass('selected');
                            $(this).addClass('selected');
                        }                        
                    });
                }
            }
        });
    }
    */

    //
    
    function bindGrid() {
        var data = {
            IN_USER_ID: "0",
            IN_BRANCH_ID: "0",
            IN_ID: "0",
            IN_PACKAGE_ID: $("#cmbSelect").val(),
            IN_STATUS: "0"                      
        };
        $.ajax({
            url: 'getPackageGrd',
            type: "POST",
            data: JSON.stringify(data),
            dataType: 'json',
            contentType: 'application/json',
            success: function (data) {
                if (data.message == 0)
                    alert("Invalid User.");
                else {
                    dataSet = data;
                    $('#example').DataTable({
                        //*****Start
                        //"lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "All"]],
                        "order": [0, "desc"], // Order data
                        //"scrollX": true,
                        //"scrollY": "500px", //Paging enable disable
                        //"scrollCollapse": true,//Scroll enable disable
                        //"paging": false, //Paging enable disable
                        //"pagingType": "full_numbers", showing all page numbers
                        //stateSave: true,
                        //*****End                                               
                        data: dataSet,
                        columns: [                        
                            { data: "ID" },
                            { data: "PACKAGE"},
                            { data: "SERVICE"},
                            { data: "STATUS"},                
                        ],
                        "columnDefs": [
                            {
                                "targets": [0],
                                "visible": false,
                                //"searchable": false
                            }                            
                        ]                        
                        // implemented export option
                        //, dom: 'Bfrtip',
                        //buttons: [
                        //    'excel', 'pdf'
                        //],
                                                 
                    });
                   
                }
            }
        });
    }

    
    $(function () {        
        $('#example tbody').on( 'click', 'tr',  (event) =>  {
            var table = $('#example').DataTable();
            if ($(event.currentTarget).hasClass('selected')) {
                $(event.currentTarget).removeClass('selected');
            }
            else {
                table.rows('.selected').nodes().each((row) => row.classList.remove('selected'));
                $(event.currentTarget).addClass('selected');
            }
                var gridData = table.row( event.currentTarget ).data();
                //$('#txtProgressNotesH').val(gridData.PROGRESS_NOTES);
                
        } );        
    }); 

    //
    /*
    function bindData() {
        $('#lblError').html('');
        var data = {
            IN_USER_ID: "1",
            IN_BRANCH_ID: "1",
            IN_ID: $("#cmbSelect").val(),
            IN_STATUS: "0"
        };
        $.ajax({
            url: 'getSubDepartmentByID',
            type: "POST",
            data: JSON.stringify(data),
            dataType: 'json',
            contentType: 'application/json',
            success: function (data) {
                if (data.message == 0)
                    alert("Invalid User.");
                else {
                    if ($("#cmbSelect").val() === '0') {           
                            $('#txtCode').val("");
                            $('#txtName').val("");
                            $('#cmbDeptName').val(0).change();
                            $('#cmbSubDepartmentCategory').val(0).change();
                            $('#txtDescription').val("");
                            $('#cmbStatus').val(1).change();   
                    }
                    else {                            
                            $('#txtCode').val(data[0].CODE);
                            $('#cmbDeptName').val(data[0].DEPARTMENT_ID).change();
                            $('#txtName').val(data[0].NAME);
                            $('#cmbSubDepartmentCategory').val(data[0].SUB_DEPARTMENT_CATEGORY_ID).change();
                            $('#txtDescription').val(data[0].DESCRIPTION);
                            $('#cmbStatus').val(data[0].STATUS_ID).change();
                    }
                }
            },
            error: function (jqXHR, exception) {
                var msg = '';
                if (jqXHR.status === 0) {
                    msg = 'Not connect.\n Verify Network.';
                } else if (jqXHR.status == 404) {
                    msg = 'Requested page not found. [404]';
                } else if (jqXHR.status == 500) {
                    msg = 'Internal Server Error [500].';
                } else if (exception === 'parsererror') {
                    msg = 'Requested JSON parse failed.';
                } else if (exception === 'timeout') {
                    msg = 'Time out error.';
                } else if (exception === 'abort') {
                    msg = 'Ajax request aborted.';
                } else {
                    msg = 'Uncaught Error.\n' + jqXHR.responseText;
                }
                alert(msg);
                return false;
            }
        });
    }
    */
</script>